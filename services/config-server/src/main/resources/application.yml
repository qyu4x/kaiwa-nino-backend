server:
  port: 8888

spring:
  profiles:
    active: native
  application:
    name: config-server
  Cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
